export interface IRegistryItem {
  id: string;
  name: string;
  url: string;
  reason: string;
  price: number;
  choped: boolean;
  imgSrc: string;
}

export interface IDetailItem {
  detail: string;
}
